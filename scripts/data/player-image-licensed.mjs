/**
 * Owner-licensed player photos stored under public/images/players/.
 *
 * Only add entries when the image is genuinely downloaded/owned and usable on FootyCompass.
 * Do NOT add stock preview URLs, watermarked thumbnails, or hotlinked previews.
 *
 * Required fields per player:
 *   imageUrl          — /images/players/{id}.webp (or .jpg/.png)
 *   imageCredit       — rights holder / photographer
 *   imageLicense      — e.g. "Licensed — FootyBrain asset library"
 *   imageSource       — e.g. "Depositphotos (licensed download)" or "Commissioned photo"
 *   imageSourceUrl    — purchase page or asset record URL (not a preview CDN)
 *   identityNote      — how name + club/nation were verified
 */
export default {
  schemaVersion: 1,
  updatedAt: '2026-05-29',
  entries: {},
};
